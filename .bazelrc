build --cxxopt=-std=c++20
build --host_cxxopt=-std=c++20
build --show_timestamps
build --announce_rc

build:cuda --define=KL_BACKEND=CUDA
build:cuda --@rules_cuda//cuda:compiler=nvcc
build:cuda --spawn_strategy=local

build:rocm --define=KL_BACKEND=ROCM