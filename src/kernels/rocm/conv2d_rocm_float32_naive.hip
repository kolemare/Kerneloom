#include <kernels/rocm/conv2d_rocm_float32_naive.hiph>

namespace kl
{

    void conv2d_rocm_float32_naive(
        const Tensor &input,
        const Tensor &kernels,
        Tensor &result)
    {
        return;
    }
}