#ifndef KL_ROCM_COPY_HIPH
#define KL_ROCM_COPY_HIPH

#include <core/tensor.hpp>

#include <cstddef>

namespace kl
{

    void rocm_copy(
        Tensor &dst,
        const Tensor &src);

    void rocm_copy_to_host(
        void *destination,
        const Tensor &source,
        std::size_t source_offset_bytes,
        std::size_t nbytes);

}

#endif // KL_ROCM_COPY_HIPH